// const fileDatabase = require('./fileDatabase')

// fileDatabase.setfilepath('./db.json')
// fileDatabase.setpropertyName('comments')

// fileDatabase.findById(1).then(console.log)


const http = require('./http')

http(routerMiddleware)